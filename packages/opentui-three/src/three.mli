(** The three.js-shaped scene graph API of opentui-three.

    Modules mirror the reference naming so demos port nearly
    line-for-line. The rendering core lives in {!Engine}. *)

module Ambient_light = Ambient_light

module Box_geometry = Box_geometry

module Cell_conversion = Cell_conversion

module Color = Color

module Directional_light = Directional_light

module Engine = Engine

module Euler = Euler

module Geometry = Geometry

module Group = Group

module Matrix4 = Matrix4
(** Column-major storage; see individual operations for aliasing rules. *)

module Material = Material

module Mesh = Mesh

module Mesh_basic_material = Mesh_basic_material

module Mesh_lambert_material = Mesh_lambert_material

module Object3d = Object3d

module Mesh_phong_material = Mesh_phong_material

module Perspective_camera = Perspective_camera

module Point_light = Point_light

module Quaternion = Quaternion

module Scene = Scene

module Vector3 = Vector3
