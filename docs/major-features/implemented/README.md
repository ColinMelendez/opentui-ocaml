# Implemented feature records

This directory contains feature records whose acceptance criteria are
satisfied by the repository. Each feature directory contains the active
`feature.md` contract and optional non-normative material under `context/`.
