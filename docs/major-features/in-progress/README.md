# In-progress feature records

This directory contains feature records whose implementation or acceptance
work is incomplete. Each feature directory contains an active `feature.md`
contract and optional non-normative material under `context/`.
